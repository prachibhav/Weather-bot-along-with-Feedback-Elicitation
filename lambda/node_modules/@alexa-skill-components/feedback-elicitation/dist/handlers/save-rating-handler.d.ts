import { HandlerInput, RequestHandler } from 'ask-sdk-core';
import { Response } from "ask-sdk-model";
import { RatingRecorder } from '../recorders/rating-recorder';
export declare class SaveRatingRequestHandler implements RequestHandler {
    private apiName;
    private ratingHandler;
    private shouldEndSession;
    constructor(apiName?: string, ratingHandler?: RatingRecorder | ((rating: number) => void), shouldEndSession?: boolean);
    canHandle(handlerInput: HandlerInput): boolean;
    handle(handlerInput: HandlerInput): Response;
}
